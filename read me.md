1. Install Node.js globally via [nodejs.org](https://nodejs.org/en).



2\. Open CMD not Windows Terminal App inside the main folder and run:

\---> npm install

\---> npx playwright install chromium



3\. Start run.bat.

